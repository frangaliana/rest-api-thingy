var React = require('react')
var ReactDOM = require('react-dom')
var App = require('./App')

//Con ello representaremos el elemento React (App) en el nodo "app" DOM del documento
ReactDOM.render(<App/>, document.getElementById('app'));
